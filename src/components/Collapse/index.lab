(let activeStyle (style.create  {
  color:"255|255|255|255",
  backgroundColor:"49|119|255|255",
  fontSize:"16px",
  lineHeight:"18px",
  height:"18px",
  margin:"0px 10px 5px 0px"
}))

(let noActiveStyle (style.create  {
  color:"170|170|170|255",
  fontSize:"14px",
  lineHeight:"18px",
  height:"18px",
  margin:"0px 10px 5px 0px"
}))

(function (Collapse props propMethod)
  (let data props.data)
  (let state (propMethod.initState {
    activeKey: undefined
  }))

  (function (changeAction obj)
    (set state.activeKey obj.key)
    (propMethod.update )
  )

   (propMethod.afterMounted (lambda (x)
    (changeAction data[0])
  ))

  <div>
    {
      (data.map (lambda (obj)
        (let isActive (== state.activeKey obj.key))
        <div key={obj.key}>
          <div 
            className={(if isActive activeStyle noActiveStyle)}
            onClick={(lambda (x) (changeAction obj))}
          >{obj.label}</div>
          {(if isActive  <div style={{margin:"0px 0px 0px 20px"}}>{obj.children}</div> undefined)}
        </div>
        )
      )
    }
  </div>
)



(export {Collapse})